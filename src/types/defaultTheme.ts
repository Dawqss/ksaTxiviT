export interface DefaultTheme {
  baseColors: {
    orange: string;
    darkBlue: string;
  };
  palette: {
    background: string;
    button: {
      background: string;
      label: string;
    };
    highlights: {
      border: string;
    };
  };
}
