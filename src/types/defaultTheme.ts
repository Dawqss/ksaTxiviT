export interface DefaultTheme {
  baseColors: {
    orange: string;
    darkBlue: string;
    white: string;
  };
  palette: {
    background: string;
    button: {
      background: string;
      label: string;
    };
    highlights: {
      border: string;
    };
  };
}
